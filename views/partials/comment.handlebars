<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      {{comment_text}}
    </div>
    <div class="text">
      --- {{user.username}} on {{format_date createdAt}}
    </div>
  </section>
  {{/each}}
</div>